Rides with the red ghost cube: every rejected placement states its reason (UI.md §1.3). Never a bare red ghost, never a generic "invalid".

```jsx
<RejectionLabel reason="Not enough budget (3 / 4)" />
<RejectionLabel reason="Would trap Ben" />
```

Pin it just below the ghost in screen space. It appears instantly — no fade in — because the Nightmare is reading it mid-drag.
