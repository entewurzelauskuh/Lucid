Directional damage vignette — a short red arc on the side the hit came from (UI.md §6, "Centre edges").

```jsx
<DamageArc from="left" intensity={0.8} />
```

Position it inside the HUD's own `position: relative` root. It is the one red thing that appears without a toast.
