namespace Lucid.Editor.Cubes
{
    /// <summary>The schema's `category` enum.</summary>
    public enum SpecCategory
    {
        Connector,
        Vertical,
        Chicane,
        Mob,
        Gimmick,
        Start,
    }
}
