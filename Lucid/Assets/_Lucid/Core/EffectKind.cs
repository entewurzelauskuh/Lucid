namespace Lucid.Core
{
    /// <summary>The Nightmare's global effects (docs/SPEC.md §10).</summary>
    public enum EffectKind : byte
    {
        Dark = 0,
        Fog = 1,
        Molasses = 2,
    }
}
